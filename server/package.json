{
  "name": "haggle-server",
  "private": true,
  "version": "1.0.0",
  "description": "Server-side negotiation engine for Haggle — WebMCP price negotiation for Shopify storefronts",
  "type": "module",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "test": "node --test test/*.test.js",
    "typecheck": "tsc --checkJs --noEmit"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "@types/node": "^20.19.43",
    "typescript": "^5.9.3"
  }
}
